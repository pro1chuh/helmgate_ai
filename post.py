import requests

token = '8260947962:AAFfnAjsPj-BLchSrdHEecELEpcdlC7a-gU'
channel = '@helmgate'

text = '''🛠 <b>Helm обновился: финансовый контроль и читаемые ответы</b>

За последние два дня мы выпустили несколько изменений, которые делают работу с AI-ассистентом прозрачнее и удобнее.

<b>Финансы — только в рублях.</b> Раньше учёт расходов на AI вёлся в долларах, что создавало путаницу при планировании бюджета. Теперь все цифры — в рублях: сколько потратили, сколько осталось, сколько уходит на каждую команду. Без пересчётов и сюрпризов от курса.

<b>Изоляция данных по компаниям.</b> Каждая организация теперь работает с отдельным ключом доступа. Данные одной компании физически отделены от данных другой — это важно и для безопасности, и для прохождения внутренних проверок и аудитов.

<b>Ответы стали удобнее читать.</b> Когда AI формирует структурированный ответ — таблицу, список, пошаговую инструкцию — он теперь отображается именно так: с заголовками, выделением и форматированием. Не сплошной поток текста, а готовый документ.

Helm должен не просто работать, а работать так, чтобы руководитель в любой момент видел расходы и контролировал доступ — без звонка в IT.

<i>— Команда Helm</i>'''

r = requests.post(
    f'https://api.telegram.org/bot{token}/sendMessage',
    json={'chat_id': channel, 'text': text, 'parse_mode': 'HTML'}
)
if not r.json().get('ok'):
    r = requests.post(
        f'https://api.telegram.org/bot{token}/sendMessage',
        json={'chat_id': channel, 'text': text}
    )
print(r.json())
