import requests

token = '8260947962:AAFfnAjsPj-BLchSrdHEecELEpcdlC7a-gU'
channel = '@helmgate'

text = '''📊 Чем больше AI-инструментов — тем хуже результат?

В 2026 году каждый второй сотрудник уже использует AI на работе. Казалось бы, производительность должна расти. Но свежие данные говорят обратное.

По данным <b>ManpowerGroup Global Talent Barometer 2026</b>: доля сотрудников, применяющих AI, выросла на 13%, а уверенность в технологии за тот же период <b>упала на 18%</b>.

Исследователи называют причину — эффект «AI brain fry»: когда сотрудник переключается между тремя и более AI-сервисами, он тратит больше сил на ориентацию в интерфейсах, чем на саму задачу. Итог: больше ошибок, выше усталость, ниже отдача от инвестиций в технологию.

Типичная картина в офисе: маркетолог пользуется одним сервисом для текстов, другим — для аналитики. HR открывает третий. Юрист вообще не знает, каким пользоваться. Каждый действует стихийно, без единого стандарта — и компания не получает ни контроля, ни реального эффекта.

<b>Это не проблема AI — это проблема хаоса в инструментах.</b>

Helm решает это иначе: один интерфейс для всей команды, который <i>сам</i> определяет, какой тип AI лучше справится с конкретным запросом. Сотрудник просто задаёт вопрос — система думает за него, какой инструмент подойдёт. Меньше переключений. Меньше путаницы. Больше результата.

<i>— Команда Helm</i>'''

r = requests.post(
    f'https://api.telegram.org/bot{token}/sendMessage',
    json={'chat_id': channel, 'text': text, 'parse_mode': 'HTML'}
)
result = r.json()
if not result.get('ok'):
    r = requests.post(
        f'https://api.telegram.org/bot{token}/sendMessage',
        json={'chat_id': channel, 'text': text}
    )
    result = r.json()
print(result)
